# banking-risk-analytics-pipeline
Build an end-to-end banking data pipeline that ingests synthetic banking data, stores raw files in GCS, loads raw tables into BigQuery, and uses dbt to create analytics-ready fraud, finance, and compliancemarts.
